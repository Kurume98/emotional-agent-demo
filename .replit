run = "cd frontend && npm install && npm run build && cd .. && gunicorn backend.app:app --bind 0.0.0.0:$PORT --workers=1 --preload --log-level=info --capture-output --enable-stdio-inheritance"
language = "python"

[env]
PORT = "5000"
FLASK_ENV = "production"
